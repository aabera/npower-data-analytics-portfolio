markdown# 🧠 Predictive Clinical Machine Learning
This folder houses predictive scripts and pipelines targeting the UCI Clinical Diabetes dataset.

## 🧪 Advanced Preprocessing Workflows
*   **Interval Midpoint Imputation:** Successfully engineered text-based range brackets (e.g., `[40-50)`) into scaled continuous variables (`45`) so the decision tree algorithm could perform mathematical evaluation.
*   **Binary Category Splitting:** Transformed categorical string groups into binary labels (`1` for readmitted, `0` for safe status).
*   **Evaluation Metrics:** Generated precision, recall, and F1-score classificat
